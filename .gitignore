# Monorepo root .gitignore.
#
# The app-level entries below deliberately have NO leading slash. A leading
# slash anchors a pattern to the directory holding the .gitignore, so a leading-
# slash "node_modules" here would no longer match apps/web/node_modules and the first
# `git add -A` after the move would stage the entire installed dependency tree.
# The terraform entries at the bottom keep their path prefix on purpose:
# infrastructure/ lives here, at the root.

# dependencies
node_modules/
.pnp/
.pnp.js

# testing
coverage/
test-results/
playwright-report/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
**/prisma/*.db

# local e2e/dev server log
.server.log

# ad-hoc local debugging scripts (smoke.local.mjs, probe.local.mjs, ...)
*.local.mjs

# real roster data — 172 named students and advisors with their university
# addresses. This repository is public, so anything committed here is served by
# raw.githubusercontent.com to anyone; that data was exactly as private as a
# repository setting, which is not a control anybody chose.
#
# apps/web/scripts/roster-source.mjs resolves ROSTER_FILE, then this file, then
# the committed synthetic fixture — so removing it from source needs no code
# change. Keep your local copy; supply it to a real seed with ROSTER_FILE.
apps/web/scripts/roster-data.mjs

# the OSE spreadsheets those names were extracted from
Tier1/*.xlsx
Clubs and Board Position.xlsx

# terraform — provider plugins (700MB+) and local state, from running
# `terraform validate` outside CI. The lock file is ignored on purpose too:
# CI pins terraform 1.9 and resolves providers itself, so a lock written by a
# newer local CLI would silently change what production deploys.
infrastructure/terraform/.terraform/
infrastructure/terraform/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfplan

# Real roster: 172 named students and advisors with university addresses.
# Never commit. scripts/roster-source.mjs falls through to the synthetic
# fixture when this is absent, so removing it breaks nothing.
apps/web/scripts/roster-data.mjs
